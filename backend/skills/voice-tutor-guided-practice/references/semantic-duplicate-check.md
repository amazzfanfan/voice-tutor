## 已问过的问题
{{asked_section}}

## 候选下一题
{{candidate_question}}

## 判断任务

判断候选题是否与任何已问题目考察同一个知识点。

判为重复：候选题只是换说法、换选项、拆出子项，或者正确答案仍是同一个对象的同一个规定、数值、禁止范围或边界条件。

不判重复：题目属于同一主题但考察不同对象、不同参数、不同行为或不同边界条件。不要仅因都涉及长度、半径、宽度、坡度、最大值、最小值或相同选项数值而判为重复。

只输出 JSON：

```json
{"duplicate": true, "reason": "一句话原因"}
```
